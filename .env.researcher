# GPT Researcher Configuration for Local Proxy
# This file configures GPT Researcher to use your local LLM proxy and DuckDuckGo search

# LLM Configuration (points to your proxy)
OPENAI_API_BASE=http://localhost:3000/v1
OPENAI_API_KEY=REPLACE_WITH_YOUR_PROXY_ACCESS_TOKEN

# Search Engine Configuration (DuckDuckGo - no API key needed!)
RETRIEVER=duckduckgo

# Optional: Local documents path
DOC_PATH=./my-docs

# Temperature for LLM responses (0.0 - 1.0)
TEMPERATURE=0.7

# Report type (default: research_report)
# Options: research_report, quick_report, subtopic_report, multi_agent_report
REPORT_TYPE=research_report

# Maximum number of sources to aggregate
MAX_SOURCES=20

# Report format
# Options: markdown, pdf, docx, json
REPORT_FORMAT=markdown
