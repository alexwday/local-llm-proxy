Flask==3.0.0
flask-cors==4.0.0
requests>=2.32.0
python-dotenv==1.0.0
litellm>=1.50.0
toml==0.10.2

# Web scraping with Crawl4AI
crawl4ai>=0.7.6
playwright>=1.40.0
pydantic>=2.0.0

# GPT Researcher (autonomous research agent)
# Note: Has 100+ dependencies, first install may take 5-10 minutes
gpt-researcher==0.14.4
langchain>=0.3.0  # Required by gpt-researcher but not auto-installed
langgraph>=0.2.0  # Required by gpt-researcher but not auto-installed

# RBC Security (install on work computers only)
# pip install rbc_security
